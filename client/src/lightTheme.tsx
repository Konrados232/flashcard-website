import { createTheme } from '@mui/material/styles';

export const lightTheme = createTheme({
    palette: {
        primary: {
            main: '#0a0a0a',
        },
        secondary: {
            // main: '#f50057',
            main: '#878787',
        },
        background: {
            default: '#f2eded',
        },
    },
});
