import { createTheme } from '@mui/material/styles';

export const darkTheme = createTheme({
    palette: {
        primary: {
          main: '#373737',
        },
        secondary: {
          main: '#f50057',
        },
        background: {
          default: '#82828a',
        },
      },
});
