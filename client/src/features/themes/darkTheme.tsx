import { createTheme } from '@mui/material/styles';

export const darkTheme = createTheme({
    palette: {
        primary: {
            main: '#373737',
        },
        secondary: {
            // main: '#f50057',
            main: '#a7a6ab',
        },
        background: {
            default: '#82828a',
        },
    },
});
